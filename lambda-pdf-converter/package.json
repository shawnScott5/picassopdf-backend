{
    "name": "picassopdf-lambda-converter",
    "version": "1.0.0",
    "description": "AWS Lambda function for PDF conversion using chrome-aws-lambda",
    "main": "index.js",
    "scripts": {
        "build": "npm install",
        "deploy": "npm run build && zip -r function.zip ."
    },
    "dependencies": {
        "chrome-aws-lambda": "^10.1.0",
        "puppeteer-core": "^10.1.0",
        "axios": "^1.6.0",
        "mime-types": "^2.1.35",
        "jsdom": "^23.0.0"
    },
    "engines": {
        "node": "20.x"
    }
}
